<Page
    x:Class="DesktopTune.View.QueuePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DesktopTune.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:DesktopTune.ViewModel"
    Title="QueuePage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition Width="0.5*" />
        </Grid.ColumnDefinitions>
        <ItemsControl Grid.Column="0" ItemsSource="{Binding MusicQueue}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Height="60" Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Картинка  -->
                        <Image
                            Grid.Column="0"
                            Width="130"
                            Height="60"
                            Source="{Binding Thumbnail}" />

                        <!--  Текст  -->
                        <StackPanel
                            Grid.Column="1"
                            Margin="5,0"
                            Orientation="Vertical">
                            <TextBlock
                                FontWeight="Bold"
                                Text="{Binding Title}"
                                TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                FontStyle="Italic"
                                Foreground="Gray"
                                Text="{Binding OwnerName}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <!--  Кнопка удалить  -->
                        <Button
                            Grid.Column="2"
                            Height="20"
                            Margin="5,0"
                            Command="{Binding DataContext.EnqueueMusic, RelativeSource={RelativeSource AncestorType=Page}}"
                            CommandParameter="{Binding}"
                            Content="Удалить" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


        <ItemsControl Grid.Column="1" ItemsSource="{Binding PriorityMusicQueue}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Height="60" Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Картинка  -->
                        <Image
                            Grid.Column="0"
                            Width="130"
                            Height="60"
                            Source="{Binding Thumbnail}" />

                        <!--  Текст  -->
                        <StackPanel
                            Grid.Column="1"
                            Margin="5,0"
                            Orientation="Vertical">
                            <TextBlock
                                FontWeight="Bold"
                                Text="{Binding Title}"
                                TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                FontStyle="Italic"
                                Foreground="Gray"
                                Text="{Binding OwnerName}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <!--  Кнопка удалить  -->
                        <Button
                            Grid.Column="2"
                            Height="20"
                            Margin="5,0"
                            Command="{Binding DataContext.EnqueueMusic, RelativeSource={RelativeSource AncestorType=Page}}"
                            CommandParameter="{Binding}"
                            Content="Удалить" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
